type Role {
  id: ID!
  name: String!
  guard_name: String!
  permissions: [Permission!]! @belongsToMany
  users_count: Int! @count(relation: "users")
  created_at: DateTime!
  updated_at: DateTime!
}
